#!/usr/bin/env bash
# displays the time for 12 hours and 59 minutes.

h=0
min=1

while [ $h -le 12 ]
do
  echo "Hour: $h"
  while [ $min -le 59 ]
  do
    echo "$min"
    min=$((min+1))
  done
  min=1
  h=$((h+1))
done
